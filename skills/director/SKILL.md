---
name: director
description: Use Factory on a specified target repository to install, inspect, run, check status, cancel, or review media for a development Objective. Never use it on Factory's own repository.
---

# Factory director

Resolve the target repository and Objective issue number from the request. Refuse any Factory source, rebuild, or archive repository as a target. Read the target repository's own instructions and Objective; they remain the authority for scope and validation. Use `factory help` for the installed CLI surface. Keep controller scheduling inside Factory rather than reproducing it in chat or editing its state.

For an install-only request, use the packaged `setup` skill and stop before execution. For planning, use `factory plan --objective N --output ABSOLUTE_PRIVATE_FILE`; show its pinned source packet, graph, review status, and any unresolved question. Inspect each proposed validation command and its source. The exact source-declared `pnpm install --frozen-lockfile --ignore-scripts` may bootstrap a fresh validation worktree before pnpm checks when the pinned target supplies a tracked lockfile; a plain install or a command absent from pinned sources has no authority. A question requires a named operator's specific answer or refusal, actor, and reason before `factory decide` records the decision. Do not call an unresolved plan runnable. For execution, confirm the installed repository binding and authority, then use `factory run --objective N --plan FILE` with a clean decided candidate. Without a plan file, `run` compiles and independently reviews a fresh graph.

Use `factory status --objective N` to report progress, failure, final validation, a result decision, or a media selection pause. If an Objective pauses for media, read candidate IDs and digests from status; use `factory review --objective N --item ID --set SET_ID --output ABSOLUTE_NEW_DIRECTORY` for human review outside the target checkout. Obtain the human's whole-set selection, use `factory select --objective N --item ID --set SET_ID` with an optional reason and explicit `--bind` for each pending direct dependent that may consume the set, then run again. Respect target-owned Git LFS rules and never substitute unselected files. Structured source bindings can name pinned repository files, Objective-cited absolute private files, or recognized GitHub Objective attachments; never invent access, source identity, rights, or tool provenance.

If status names a pending Work Item or final Objective result criterion, show its exact tree, question, and evidence to the operator. The review packet uses bounded text excerpts with explicit truncation markers and blob descriptors for opaque files. A truncated text excerpt cannot auto-pass even if independent review reports pass. Show the affected paths and have the operator inspect the full tree before accepting; a larger `FACTORY_RESULT_REVIEW_TEXT_BUDGET_BYTES` and reviewer context may allow a fresh complete review. Obtain explicit acceptance or refusal and reason, then record `factory decide-result --objective N [--item ID] --tree EXACT_TREE_SHA --outcome accept|refuse --actor NAME --reason TEXT`. Include `--item` for a Work Item; omit it for final Objective acceptance. Resume with `factory run --objective N` only after acceptance. A refusal stops delivery, and the decision applies only to that criterion and tree. Do not infer human acceptance from diagnostic events.

For an agent-readable current state, use `factory status --objective N --json`. For a private timeline, use `factory diagnostics --objective N` or add `--follow` while a run is active. Use `factory logs --objective N --item ID [--follow]` for that attempt's worker stdout/stderr. Stop following when enough evidence is available. These outputs may include private validation and worker content, so do not paste or publish them without review. The atomic snapshot returned through status remains the continuation authority; diagnostics never authorize retry or imply successful completion.

Use `factory cancel --objective N` for an authorized stop and `factory retry --objective N --item ID` only for an explicitly authorized new attempt. Report the resulting status or refusal and return; do not poll indefinitely. Managed-agent and sandbox execution remain unavailable.
