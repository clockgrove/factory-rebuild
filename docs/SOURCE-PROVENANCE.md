# Source provenance

The clean implementation starts from the Factory plan reviewed September 22, 2026. The archived source at `clockgrove/factory` commit `994bbfcadb317aed2dfa932ec9d128e7d0d8c7a8` is reference material only. No runtime source, test, fixture, snapshot, or generated artifact has been copied into Slice 0.

The MIT license text is retained from the archived repository. Runtime code does not read this ledger. Add a precise path and destination here before extracting an archived algorithm or concrete API behavior in later slices.
