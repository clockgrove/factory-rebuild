# Factory archive-to-rebuild capability review

Status: operator-approved capability audit, September 22, 2026. The accepted pre-pilot trunk closure list is issues #19–#25 and #28, followed by the #26 pilot gate. T03, T11, and T26 were narrowed as noted below. Acceptance does not mean implementation is complete, and no row authorizes copying the archived implementation wholesale.

## Evidence and decision rule

The archived reference is `clockgrove/factory` at `994bbfcadb317aed2dfa932ec9d128e7d0d8c7a8`. I reviewed its `docs/DESIGN.md`, `docs/OPERATING-REFERENCE.md`, `docs/CONFORMANCE.md`, `docs/OBJECTIVE-ASSETS.md`, `docs/LARGE-FILES.md`, `docs/THREAT-MODEL.md`, `docs/OBJECTIVE-AUTHORITY.md`, and `docs/DELIVERY-PLAN.md`, then checked the named implementation paths below. The rebuild reference is `clockgrove/factory-rebuild` at `5aa5b28`, its public implementation plan and build status, its source-provenance ledger, current code, and the closed slice evidence. Archive paths below are relative to that pinned archived commit. Public issue links point to the implementation/acceptance owner; they are not evidence that the behavior is already complete.

`Proven` means a bounded installed or application-integration path exists in the public build status; it does **not** assert all edge cases or a published release. `Partial` means a narrower behavior exists but the recommended contract is not yet proven. `Absent` means no public rebuild surface for the capability. `Branch` and `Leave out` are scope decisions. Every retain/replace decision means **keep the product guarantee, not the archived protocol, limits, test file, or module topology**. The operator approved the decisions in this matrix; an implementing agent must still follow each linked issue's acceptance scope.

The archived paths are provenance, not required implementation instructions. An unrelated contributor can use this matrix, the public plan, and the linked issues without access to the archived repository.

## Accepted shippable trunk decisions

| ID  | Capability and archived evidence                                                                                                                                                                       | Rebuild evidence / gap                                                                                                                                                                      | Accepted decision and proof owner                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T01 | Target-installed open-source plugin, no hosted dependency: `docs/DESIGN.md` product contract and `docs/DELIVERY-PLAN.md` release boundary.                                                             | Manifest, CLI, setup/director skills, tarball install and disposable Objectives exist; no published artifact accepted.                                                                      | **Retain. Partial.** Publish one versioned third-party artifact; install from clean target using public instructions. [#25](https://github.com/clockgrove/factory-rebuild/issues/25).                                                                                                                                                                                                                                             |
| T02 | Explicit activation and repository authority: `docs/DESIGN.md` security/activation; `docs/THREAT-MODEL.md`.                                                                                            | `src/config.ts` refuses Factory targets and unsupported modes; `src/runner.ts` uses one active Objective/installation lock. Slice 1/2 tests cover the ordinary path.                        | **Retain, simplify. Proven ordinary path.** Keep explicit start, one target, one mode, one active Objective; do not restore lease/election protocols. Preserve self-target refusal in packaged smoke.                                                                                                                                                                                                                             |
| T03 | Agent-facing chat/MCP/CLI surface: `src/cli.ts`, `src/mcp-server.ts`, `docs/OPERATING-REFERENCE.md`.                                                                                                   | CLI/skills exist; no public preflight/preview and the first-release CLI/skills boundary is now selected.                                                                                    | **Retain outcome, replace interface. Partial.** **Approved:** documented CLI and installed use skills over one application layer. No MCP adapter in the first release; reconsider only for a demonstrated agent-integration need. [#19](https://github.com/clockgrove/factory-rebuild/issues/19), [#25](https://github.com/clockgrove/factory-rebuild/issues/25), [#28](https://github.com/clockgrove/factory-rebuild/issues/28). |
| T04 | Pinned, repository-grounded Objective planning and read-only plan: `src/application/plan.ts`, `src/application/compiler-preflight.ts`, `docs/DESIGN.md` compiler sections.                             | `src/compiler.ts` reads Objective plus checkout-root `AGENTS.md`/`README.md`; no public pinned canonical-source selector or zero-mutation preview.                                          | **Retain, simplify. Partial.** Target selects exact paths/headings; compiler reads pinned base; preview and run bind the same candidate or explicitly recompile. [#19](https://github.com/clockgrove/factory-rebuild/issues/19).                                                                                                                                                                                                  |
| T05 | Independent compiler judgment: `src/management/draft-compilation.ts`, `src/compiler/judge-context.ts`.                                                                                                 | Structured compiler and deterministic graph check exist; independent reviewer does not.                                                                                                     | **Retain outcome, replace pipeline. Absent.** One source-backed review, at most one evidenced revision, then specific human fallback. Do not restore obligation-extraction or recursive repair machinery. [#19](https://github.com/clockgrove/factory-rebuild/issues/19).                                                                                                                                                         |
| T06 | Work Item DAG, source citations, owned paths and resources: `src/compiler/index.ts`, `src/graph-analysis.ts`, `docs/DESIGN.md` worker contract.                                                        | `src/contracts.ts`, `src/scheduler.ts`, Slice 1/2 and integration tests establish the core graph and conflict rules.                                                                        | **Retain, simplify. Proven ordinary path.** Keep the small Work Item schema and actual target-required brief; no provider/driver policy in a Work Item.                                                                                                                                                                                                                                                                           |
| T07 | Source-grounded validation-command authority: archived `src/compiler/validation-design.ts`, `src/application/compiler-preflight.ts`; `docs/DESIGN.md` validation.                                      | `src/compiler.ts` checks source-declared commands by substring and does not authenticate base-observed commands.                                                                            | **Retain, simplify. Partial.** Tie each command to exact pinned source/base evidence; preview host execution authority; reject invented commands. [#20](https://github.com/clockgrove/factory-rebuild/issues/20).                                                                                                                                                                                                                 |
| T08 | Native Work Item issue/dependency projection and idempotency: `src/graph.ts`, `src/github.ts`, `docs/OBJECTIVE-AUTHORITY.md`.                                                                          | `src/github.ts` creates issue markers and native dependencies, but partial projection/response loss is not accepted.                                                                        | **Retain, simplify. Partial.** One stable issue per graph ID, no duplicate dependency, stop on ambiguous external identity; no receipt ledger. [#22](https://github.com/clockgrove/factory-rebuild/issues/22).                                                                                                                                                                                                                    |
| T09 | Dependency-ready, path/resource-safe local concurrency: archived `src/graph-analysis.ts`, `docs/DESIGN.md` scheduling.                                                                                 | `src/scheduler.ts` and regular delivery prove concurrent independent roots; `src/delivery/native-runner.ts` prepares only initial roots concurrently.                                       | **Retain, simplify. Partial in native mode.** Admit later ready waves within operator/driver slots; serialize only actual conflicts/publication. [#21](https://github.com/clockgrove/factory-rebuild/issues/21).                                                                                                                                                                                                                  |
| T10 | Restart, cancellation, explicit retry and owned worker cleanup: archived `src/runtime/process-group.ts`, `src/backends/codex-sdk-local.ts`.                                                            | Slice 2 proves ordinary active restart/cancel/retry. Native failure may leave an item `running` when Objective errors.                                                                      | **Retain, simplify. Partial.** Fix native terminal failure/retry and preserve exact-attempt identity; no general recovery coordinator. [#21](https://github.com/clockgrove/factory-rebuild/issues/21).                                                                                                                                                                                                                            |
| T11 | Local worker trust boundary: `docs/THREAT-MODEL.md`, `src/runtime/pinned-git-environment.ts`, `src/execution/artifacts.ts`.                                                                            | `src/execution/local.ts` checks owned paths and strips conventional credentials; it over-rejects unchanged symlinks/submodules, and prepublication suspected-secret coverage is incomplete. | **Approved:** check changed candidate hazards, worker environment, and changed-content secrets in the isolated local worktree before any remote branch/LFS upload or PR. Do not scan unrelated existing target files or build a custom DLP engine; trusted-local execution is not hostile-code containment. Partial pending [#24](https://github.com/clockgrove/factory-rebuild/issues/24).                                       |
| T12 | Exact collected changes and large binary bytes: archived `src/runtime/artifact-patch.ts`, `src/execution/artifact-content.ts`, `docs/LARGE-FILES.md`.                                                  | `src/execution/local.ts`, `src/content/local.ts`, media/LFS path and Slice 4/5 prove exact bytes on the selected path.                                                                      | **Retain, simplify. Proven selected path.** Keep Git tree/digest identity and content-addressed bytes; discard old chunk-transfer receipts and Factory-owned byte/file ceilings.                                                                                                                                                                                                                                                  |
| T13 | Independent fresh-tree command validation: archived `src/validation/clean-run.ts`, `src/validation/evidence.ts`.                                                                                       | `src/validation.ts` runs commands from a fresh exact-tree checkout; Slice 1–5 evidence covers this.                                                                                         | **Retain. Proven command path.** Continue binding commands/result tree; see T14 for what command success cannot establish.                                                                                                                                                                                                                                                                                                        |
| T14 | Criterion-level semantic acceptance separate from worker self-report: archived `src/management/codex-cli.ts` semantic reviewer and `docs/DESIGN.md` validation.                                        | `src/runner.ts` treats command success as Work Item/Objective acceptance; no bounded result reviewer.                                                                                       | **Retain outcome, replace machinery. Absent.** Automated source-backed semantic review where commands are insufficient; one specific human fallback for uncertainty/authority. [#20](https://github.com/clockgrove/factory-rebuild/issues/20).                                                                                                                                                                                    |
| T15 | Regular PR delivery and exact head/check discipline: archived `src/publication/publisher.ts`, `src/github.ts`.                                                                                         | `src/delivery/regular.ts`, `src/github.ts`, Slice 1/5 live evidence.                                                                                                                        | **Retain. Proven ordinary path.** Keep target branch protection/checks authoritative and stop on changed identity; finalization interruption is T18.                                                                                                                                                                                                                                                                              |
| T16 | Native linear PR stacks with fork/join boundaries: archived `src/publication/delivery.ts`, `src/publication/github-stacks.ts`, `docs/PLATFORM-BEHAVIOR.md`.                                            | `src/delivery/plan.ts` and native stack adapter passed a predetermined installed stack.                                                                                                     | **Retain. Proven linear path, partial scale.** Do not restore cascade repair or broad qualification matrices. Later dependency waves are T09/[#21](https://github.com/clockgrove/factory-rebuild/issues/21).                                                                                                                                                                                                                      |
| T17 | Exact predecessor-base replay/revalidation before publication or merge: archived `src/publication/publisher.ts`, `docs/DESIGN.md` integration.                                                         | Regular/native code and integrated tests cover expected heads and replay; complex external races are not claimed.                                                                           | **Retain. Proven bounded path.** Keep exact tree/base/head checks; stop on ambiguity instead of implementing archived transaction protocol. [#21](https://github.com/clockgrove/factory-rebuild/issues/21), [#22](https://github.com/clockgrove/factory-rebuild/issues/22).                                                                                                                                                       |
| T18 | Completion after GitHub side effects: archived `src/graph.ts`, `src/github.ts`, `docs/OBJECTIVE-AUTHORITY.md`.                                                                                         | Current completion can persist `done` before issue closure; a closure failure is not a settled Objective.                                                                                   | **Retain, simplify. Partial.** Replay the exact pending closure without rerunning workers or changing the validated result. [#22](https://github.com/clockgrove/factory-rebuild/issues/22).                                                                                                                                                                                                                                       |
| T19 | Objective-level acceptance at exact integrated head: archived `docs/DESIGN.md` definition of done and validation.                                                                                      | Slice 1–5 run final commands at the observed head; semantic exit criteria still lack a gate.                                                                                                | **Retain. Partial.** Final command and semantic evidence must both bind to the same integrated head; child completion never implies Objective completion. [#20](https://github.com/clockgrove/factory-rebuild/issues/20).                                                                                                                                                                                                         |
| T20 | Objective media ingress: archived `src/assets/import.ts`, `docs/OBJECTIVE-ASSETS.md`.                                                                                                                  | `src/media.ts` imports worktree files; no safe local/private or recognized GitHub Objective-attachment import through the public plugin.                                                    | **Retain, simplify. Partial.** Import authorized private/local and recognized attachment bytes once with digest/source/visibility; no arbitrary URL scraper or old transfer protocol. [#23](https://github.com/clockgrove/factory-rebuild/issues/23).                                                                                                                                                                             |
| T21 | Format-neutral multi-file candidates and immutable content: archived `src/media/contracts.ts`, `src/media/execution.ts`, `docs/OBJECTIVE-ASSETS.md`.                                                   | `src/media.ts`, `src/content/local.ts`, Slice 4/5 prove whole-set image-plus-sidecar capture and selection.                                                                                 | **Retain. Proven selected path.** Keep opaque audio/3D/video support at the asset boundary, without format-specific generators or a second orchestrator. Extend proof with one non-image set in [#23](https://github.com/clockgrove/factory-rebuild/issues/23).                                                                                                                                                                   |
| T22 | Selection decision, provenance, rights, visibility and lineage: archived `src/media/review.ts`, `src/media/contracts.ts`.                                                                              | Descriptors exist, but `src/contracts.ts`/`src/media.ts` omit full actor/time/reason/destination/binding decision and some harness identity.                                                | **Retain, simplify. Partial.** Preserve only supplied authoritative metadata; record exact approved set and selector. [#23](https://github.com/clockgrove/factory-rebuild/issues/23).                                                                                                                                                                                                                                             |
| T23 | Approved assets bound into dependent harness attempts: archived `src/assets/media-intent.ts`, `docs/OBJECTIVE-ASSETS.md`.                                                                              | Source bindings exist for repository files; private content-store-to-downstream binding is not yet proven.                                                                                  | **Retain, simplify. Partial.** Deliver only selected assets as files/descriptors; adapter owns model modality/tool calls. [#23](https://github.com/clockgrove/factory-rebuild/issues/23).                                                                                                                                                                                                                                         |
| T24 | Target-owned Git LFS assignment, pointer/upload and exact hydration: archived `src/repository-profiles/git-lfs.ts`, `src/publication/git-lfs-output.ts`, `docs/LARGE-FILES.md`.                        | Slice 4/5 live disposable Objectives verify selected pointer and fresh-clone bytes.                                                                                                         | **Retain. Proven disposable path.** No MIME-inferred policy, automatic `.gitattributes` mutation, hidden Factory size caps, or history rewrite. Real Clockgrove evidence remains [#26](https://github.com/clockgrove/factory-rebuild/issues/26).                                                                                                                                                                                  |
| T25 | Agent-readable status and explanation: archived `src/application/status.ts`, `src/application/explain.ts`, `docs/OPERATING-REFERENCE.md`.                                                              | Current CLI status is a summary; private worker output exists but no correlated read/follow timeline.                                                                                       | **Retain outcome, replace receipt-derived implementation. Partial.** Machine-readable state plus local structured diagnostics; optional later OTel exporter, not a recovery journal. [#28](https://github.com/clockgrove/factory-rebuild/issues/28).                                                                                                                                                                              |
| T26 | Credential-free CI and installed-package evidence: archived `docs/CONFORMANCE.md`, `docs/DELIVERY-PLAN.md`.                                                                                            | `quality.yml`, PR #16 integration suite, packed-install and disposable live gates exist. The archived six-gate qualification system is not retained.                                        | **Approved:** credential-free integration CI, one exact-public-artifact disposable Objective, and the Clockgrove third-party pilot. Proven development gate; public release evidence remains incomplete. Do not restore old fault/host/provider matrices. [#25](https://github.com/clockgrove/factory-rebuild/issues/25).                                                                                                         |
| T27 | Public release/community assets and third-party onboarding: archived `docs/DELIVERY-PLAN.md`, `LICENSE`, `SECURITY.md`, `SUPPORT.md`, `CODE_OF_CONDUCT.md`, `GOVERNANCE.md`, plugin/marketplace files. | Public repo has a development package, README, fixture and build docs; complete release assets and published install are unaccepted.                                                        | **Retain outcome, recreate assets. Partial.** Explicit release checklist and approved omissions, no inherited protocols. [#25](https://github.com/clockgrove/factory-rebuild/issues/25).                                                                                                                                                                                                                                          |
| T28 | First adopter through the same public plugin: archived `docs/CONFORMANCE.md` installed-application principle.                                                                                          | Disposable objectives are proven; private Clockgrove W0-001/LFS has not run through a published public artifact.                                                                            | **Retain acceptance gate. Absent.** Use only the public third-party interface; keep private adopter evidence out of public implementation authority. [#26](https://github.com/clockgrove/factory-rebuild/issues/26).                                                                                                                                                                                                              |

## Named later branches and deliberate exclusions

| Capability and archive reference                                                                                                                                                 | Proposed disposition                                                                                       | Rationale / trigger                                                                                                                                                                                             |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Managed coding-agent execution: `src/backends/github-copilot.ts`, `docs/PROVIDER-QUALIFICATION.md`.                                                                              | **Branch:** [#7](https://github.com/clockgrove/factory-rebuild/issues/7).                                  | `ManagedExecutionDriver` must prove the provider-owned task/result contract. Evaluate GitHub Copilot SDK cloud sessions, not its local mode; do not port archived Copilot lifecycle assumptions.                |
| Sandbox execution plus chosen agent harness: `src/backends/vercel-sandbox.ts`, `src/backends/sandbox-common.ts`.                                                                 | **Branch:** [#8](https://github.com/clockgrove/factory-rebuild/issues/8).                                  | After #55 lands, `SandboxExecutionDriver` composes `SandboxProvider` with trunk's installed `AgentHarness` seam; it does not design another BYO harness interface or emulate a managed agent.                   |
| Daytona: `src/backends/daytona.ts`.                                                                                                                                              | **Branch:** [#9](https://github.com/clockgrove/factory-rebuild/issues/9).                                  | First sandbox provider after the provider-neutral path works; no trunk Daytona qualification.                                                                                                                   |
| Local Codex CLI fallback and App Server durable sessions: `src/backends/codex-cli-local.ts`, `src/backends/codex-app-server.ts`, `docs/CODEX-APP-SERVER-SESSIONS.md`.            | **Defer these archived Codex alternatives.**                                                               | Trunk [#55](https://github.com/clockgrove/factory-rebuild/issues/55) proves the installed local harness seam with a separate provider; it does not justify porting the archived Codex CLI/App Server protocols. |
| Provider budgets, economics and accounting: `src/control/budget.ts`, `src/economics/`, `docs/RUNTIME-ECONOMICS.md`.                                                              | **Defer provider-specific authorization to the paid-execution branch; leave old accounting protocol out.** | Local trunk need not invent pricing or paid-resource receipts. A paid provider must have explicit operator authority and truthful usage before activation.                                                      |
| Multi-Objective capacity/fairness, adaptive pressure, bursting, shared controller: `src/controller/`, `src/scheduling/`, `docs/ADAPTIVE-SCHEDULING.md`.                          | **Leave out of trunk.**                                                                                    | One installation/Objective and operator-selected local concurrency are explicit current boundaries. Revisit only after real multi-Objective or cloud-scale need.                                                |
| Service manager, process election and unattended-controller qualification: `src/service/`, `docs/HOST-SCHEDULING.md`.                                                            | **Defer optional host integration.**                                                                       | Explicit foreground controller/restart path suffices; installation must not silently start a service.                                                                                                           |
| Media production providers, Sharp transforms, capture catalog and tool discovery: `src/media/adapter.ts`, `src/assets/handlers.ts`, `docs/OBJECTIVE-ASSETS.md` advanced capture. | **Leave out of Factory trunk.**                                                                            | Harness owns configured MCP/function calls; a separate media-tools package/MCP needs a concrete game-dev workload first. Preserve asset lifecycle, not Factory tool orchestration.                              |
| Searchable asset index/vector catalog: archived media manifests.                                                                                                                 | **Leave out.**                                                                                             | A later rebuildable local index may derive from manifests/store/repo when a concrete retrieval need appears; it is not canonical state.                                                                         |
| Automatic cross-repository finding publication: `src/control/finding-reporting.ts`, `docs/FINDING-REPORTING-QUALIFICATION.md`.                                                   | **Leave out.**                                                                                             | Not needed to complete an Objective; adds cross-repo authority and another publication lifecycle. Manual issue creation remains available.                                                                      |
| Event/receipt journal, Git refs for state, leases, epochs, takeover and deep recovery: `src/control/`, `src/recovery/`, `src/supervisor.ts`.                                     | **Discard.**                                                                                               | One atomic local snapshot and exact external identity observation are the approved replacement. Ambiguity stops for an operator.                                                                                |
| Existing arbitrary file/byte/count ceilings and old replay/qualification matrix: `docs/LARGE-FILES.md`, `src/evaluation/`, `test/`.                                              | **Discard.**                                                                                               | Use dependency/operator limits, fresh integration tests and one installed release path; retain specific security checks without copying the test empire.                                                        |
| Archived config/state/protocol compatibility: `src/protocol/`, old config migration paths.                                                                                       | **Discard.**                                                                                               | Clean schema version 1 start; no transition edition, old-state reader, compatibility alias or active-run adoption.                                                                                              |

## New mismatch found during this review

`src/contracts.ts` still says `sourceAssets?: (string | SourceAssetBinding)[]` and calls string entries “Legacy path entries” readable in schema-version-1 snapshots. `src/compiler.ts` and `src/media.ts` still branch on that string form. That conflicts with the clean-start/no-backward-compatibility decision. The operator approved folding a **small trunk cleanup** into [#23](https://github.com/clockgrove/factory-rebuild/issues/23): use the one structured binding form in new graphs/state and remove the string adaptation, after checking current fixture/state consumers. Do not add migration machinery.

## Acceptance before further branch work

1. The operator accepted the retain/replace/defer/discard decisions and the resulting pre-pilot trunk closure list [#19–#25 and #28](https://github.com/clockgrove/factory-rebuild/issues). The archive is not automatically the specification.
2. For each retained partial/absent row, the linked issue or a separately approved issue states the smallest implementation and integration proof. Existing slice evidence is not relabeled as proof of a broader behavior.
3. Audit the merged diff and the public `BUILD-STATUS.md` against this matrix before calling the trunk shippable. The public repo, not this conversation or a private local artifact, must contain the final decision record and contributor instructions.
4. Publish/install the exact public artifact and complete the private Clockgrove pilot through that public surface. Only then start the named cloud/sandbox branches or repository cutover.
