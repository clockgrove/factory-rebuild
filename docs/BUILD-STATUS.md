# Factory build status

Updated September 25, 2026. This page is the handoff for a contributor starting with only this public repository. Use [the implementation plan](IMPLEMENTATION-PLAN.md) for scope and acceptance, [the project board](https://github.com/orgs/clockgrove/projects/2) for issue status, and [the provenance ledger](SOURCE-PROVENANCE.md) for archived-source references.

The [archive-to-rebuild capability review](ARCHIVE-CAPABILITY-DECISIONS.md) records the operator-approved product-scope reconciliation. Acceptance of a capability is not a claim that it is implemented.

The historical `clockgrove/objective-fixture` acceptance target is private. Its linked Issues and PRs below are available only to maintainers; the [fixture files](../test/fixtures/disposable-target/) and [Objective templates](../test/fixtures/objectives/) in this public repository let anyone reproduce the gates in a repository they control.

**Public v0.1.0 artifact:** The [GitHub Release](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.0) and immutable tag point to accepted source commit `ad42938d4f3218c1cd8bc3ca59a19489321bbea5`. `clockgrove-factory-0.1.0.tgz` has SHA-256 `ea5b34e47902b4d4b7ecfc9d213abcd6d0dd895bf5f3148c16938a63c1770eb5` (151,280,399 bytes). The matching `SHA256SUMS` asset verifies after a fresh public download. A clean build from the tagged tree reproduced that digest, passed all 54 tests and the merged [Quality gate](https://github.com/clockgrove/factory-rebuild/actions/runs/35888280519), and installed offline with an empty npm cache. A fresh isolated Codex home installed and enabled `factory@clockgrove` version `0.1.0` from marketplace ref `v0.1.0`. Its exact-artifact disposable Objective passed below; Clockgrove adoption remains a separate gate.

| Slice | State | Accepted evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0     | Done  | Initial clean package commit `210701a`; issue [#1](https://github.com/clockgrove/factory-rebuild/issues/1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 1     | Done  | [PR #10](https://github.com/clockgrove/factory-rebuild/pull/10); installed disposable Objective #1 completed at integrated head `b1efede79d8ac308f9bda439c944be9cdc00d8d3`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2     | Done  | [PR #11](https://github.com/clockgrove/factory-rebuild/pull/11); disposable DAG Objective #4, restart Objective #11, cancel/retry Objective #14; all final validation passed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 3     | Done  | [PR #13](https://github.com/clockgrove/factory-rebuild/pull/13); installed disposable Objective [#25](https://github.com/clockgrove/objective-fixture/issues/25) created native stack #32 with PRs #29–31 and passed final validation at `6dcbc8b270e990b14b6b035b14016526a4e26822`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 4     | Done  | Installed disposable [Objective #33](https://github.com/clockgrove/objective-fixture/issues/33) selected one complete multi-file set and hydrated LFS at `143dd5f102386296c8b613d75dc37d6c8f5b9c72`. [PR #14](https://github.com/clockgrove/factory-rebuild/pull/14) merged. Fresh corrected [Objective #38](https://github.com/clockgrove/objective-fixture/issues/38) required two sets and LFS for the image role, selected candidate B, and passed final validation and hydration at `b56ae996049de7e4787c3124f9842108739c8bbd`. Exact pinned PR-head package then passed [Objective #43](https://github.com/clockgrove/objective-fixture/issues/43), selected candidate A, and verified final head/LFS hydration at `40a312f455c60d15969e33fedab67ff166dbdfbe`. |
| 5     | Done  | [PR #15](https://github.com/clockgrove/factory-rebuild/pull/15). One pinned installed tarball (SHA-256 `eb1faf9a4ecf46bf9af3ff12ba98fab9e84531bb594a23991ec6e45a4940b90d`) completed fresh [Objective #69](https://github.com/clockgrove/objective-fixture/issues/69): concurrent independent starts, native stack #82 (PRs #79–81), two captured AssetSets, selected candidate A, target LFS policy and hydration, and all final commands at exact integrated head `b31c4aacd0875d0668f51866fb726bef183b80ec`. Earlier mixed-package Objective #48 and fixed-package media Objective #63 remain supporting evidence.                                                                                                                                                |

**Current checkpoint:** Slice 5 is done as a public slice: PRs #15 and #16 merged, and the Quality workflow passed on `main` at `95692d9`. PR #15 passed its one-immutable-package disposable gate using public fixture inputs. Objective #69 started its two independent workers from base `f9732aff67f21ac18feea76f52fa085c79f17de5` within 84 milliseconds; all seven Work Items completed under the same pinned installation. Native stack #82 merged, candidate A was reviewed and selected, the final tree `1aa3e801de97401dd956f4048ab4f6682e5899ea` passed eight source-declared commands, and fresh-clone LFS hydration verified the selected bytes. The target default branch and recorded integrated head match. This proves the Slice 5 combined gate on public inputs, not overall trunk completion.

**Published-artifact checkpoint (issue #25):** Public [release `v0.1.0`](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.0) is pinned to commit `ad42938d4f3218c1cd8bc3ca59a19489321bbea5` and tarball SHA-256 `ea5b34e47902b4d4b7ecfc9d213abcd6d0dd895bf5f3148c16938a63c1770eb5`. The downloaded artifact installed with an empty npm cache, and the isolated marketplace/plugin installation reported enabled Factory 0.1.0. Installed-package disposable Objective run `2a87b326-4e3b-4f21-8431-5c3b39632c2d` completed seven Work Items: independent roots started 23 ms apart from base `adb40b162bb9aac56ee06e7d1824a18b10e20c14`, the three-layer native stack and target LFS policy merged, and the operator selected whole AssetSet A. Its last media PR was private fixture PR #16; Factory reported `state=complete`, `objectiveClosure=complete`, `finalValidation=true`, and exact integrated head `696d7d93dfc1bd08cee2b55aab127075cbc84c1b`, with selected-byte fresh-clone LFS verification. The [#25 acceptance record](https://github.com/clockgrove/factory-rebuild/issues/25) contains the operator decision and detailed identities. This proves the old published artifact's generic gate, not the separate Clockgrove pilot or any later Factory change.

**Application integration evidence:** [PR #16](https://github.com/clockgrove/factory-rebuild/pull/16) added a credential-free application test gate with scripted planning/harness adapters, a stateful GitHub-domain fake, and temporary Git repositories. Its original 22 tests covered concurrent regular DAG execution, restart/cancel/retry, native stack/replay, AssetSet selection, LFS hydration, and packed install/status. The combined code also passed installed [Objective #85](https://github.com/clockgrove/objective-fixture/issues/85) from one tarball (SHA-256 `e1ac461d3cc56c0fd03525734e958ad033cb2a3d8a3e9ef2ffbbc186e0fa7137`), with final validation at `1819991140c1cd9657594716ad5e2c34d926fd9d` and reviewed LFS bytes. Later issue-specific tests extend this gate; no new public-artifact release or private-adopter claim follows from those deterministic tests.

**Pinned planning checkpoint (issue #19):** The application and CLI now expose read-only `plan` over Objective text plus exact canonical paths/headings from the pinned Git base. The preview contains full accepted source content/digests, graph and command authorization status. A separate model thread reviews the same packet, with one sourced revision/re-review and a specific recorded human decision when needed. A clean candidate binds Objective, base, sources and graph for `run --plan`; a changed input stops activation. Focused temporary-Git tests cover dirty-checkout disagreement, missing headings, zero run-state mutation, sourced findings, fallback, blocked commands and accepted-plan reuse. Exact command trust and result-level semantic acceptance are the next checkpoint below.

**Validation acceptance checkpoint (issue #20):** The plan preview checks source-declared commands as complete lines in their exact pinned citation, base-observed commands against a named tracked base file or matching `package.json` script, and includes final Objective commands. Unsupported commands remain visible but block run. Runtime rechecks authority on resumed state and pins selected package-manager scripts before execution. Each Work Item and final Objective result receives a separate read-only criterion review tied to the exact validated tree, pinned sources, Git change, and command evidence. A clean sourced verdict passes automatically; missing or conflicting evidence holds a specific criterion for `decide-result` with exact tree, actor, time, outcome, and reason. Zero commands alone do not mark a result accepted. The existing temporary-Git application gate plus focused trust, verdict, decision, refusal, and tree-binding tests pass locally. Live published-artifact acceptance remains a later release gate.

**Package-script follow-up (issue #20):** npm and pnpm validation pins each directly selected script and pre/post hooks plus relevant package-manager configuration at the accepted base. It blocks result hook or selected-script changes before host execution while allowing package metadata, dependency, and unrelated script edits. Nested package-manager wrappers stop for separate authority. One exact source-declared `pnpm install --frozen-lockfile --ignore-scripts` command may bootstrap a fresh validation worktree from a tracked lockfile; plain install and pnpmfile hooks remain blocked. The result reviewer receives every exact-tree change through bounded text excerpts with explicit truncation, changed blob IDs/sizes, selected asset descriptors, and validation evidence; an opaque binary is not encoded into the prompt. Reviewer failure appears in the pending criterion detail. A model pass over any truncated text excerpt also waits for an exact-tree operator decision or a retry with fuller reviewer context. The focused temporary-Git gate covers npm, pnpm, changed hooks, a nested wrapper, safe bootstrap preview, a benign metadata/dependency edit, and a large binary review packet. Candidate code invoked by an unchanged, source-authorized script still runs locally under target authority; this is not a sandbox.

**Diagnostics checkpoint (issue #28):** [PR #37](https://github.com/clockgrove/factory-rebuild/pull/37) merged. The CLI `status --json` projects current state from the atomic snapshot, including ready/blocked reasons and exact Git and GitHub identities. `diagnostics` reads/follows a private NDJSON timeline of planning, scheduling, harness, validation output, GitHub delivery, media decisions, and finalization. Its original SDK progress/usage proof covered the Work Item harness only; it did not preserve production planning or reviewer adapter telemetry. The log is never replay authority; write loss is visible on stderr. OpenTelemetry remains an optional future exporter of safe event metadata, with no hosted backend in this slice.

**Model-invocation diagnostics acceptance checkpoint (issue #73):** [PR #74](https://github.com/clockgrove/factory-rebuild/pull/74) merged exact reviewed source head `ce252ce72fe1cb7bd60c6f28d78e7823bd00102f` as main commit `afa68180a53e356e00642bcf2b5a180d6598628b`. It adds a provider-neutral observation contract carrying compile, graph-review, Work Item result-review, and final Objective-review start/progress/terminal events from the model adapter to the private application timeline. The Codex adapter streams real thread/turn/item events, explicit configured model and reasoning, provider thread identity, safe byte counts and digests, exact supplied usage categories, explicit unavailable usage, and parse/provider failure classes. Deterministic compiler and acceptance checks add semantic-validation rejection fields without changing lifecycle authority. `factory diagnostics --summary` derives Objective, phase, and invocation-scope counts, available token totals, unavailable counts, and cache-read ratio with its numerator and denominator. The full local gate passed all 80 tests plus build, typecheck, lint, formatting, notices, package dry-run, and `git diff --check`; hosted [Quality run 36078593707](https://github.com/clockgrove/factory-rebuild/actions/runs/36078593707) passed on that exact source head, and the final independent review reported no remaining findings. The packed test installs the candidate into an isolated prefix, drives planning against a disposable temporary-Git target, and reads exact supplied usage plus aggregates through the public CLI. This is accepted deterministic packed-artifact evidence, not a new public release or live-provider qualification; exact public-release and private-pilot proof remain later gates coordinated with #55 and #26.

**Graph-review source-grounding correction checkpoint (issue #77):** [PR #78](https://github.com/clockgrove/factory-rebuild/pull/78) merged exact reviewed source head `d15606ee79fa475ee4bce8246c465a4cd9c47bd1` as main commit `e4feb29acc5e9f6eed8bd19f21ee19e2ac7bd968`. Read-only planning for public Clockgrove [Objective #188](https://github.com/clockgrove/clockgrove/issues/188) exposed a prompt/schema mismatch: the reviewer twice returned heading-qualified labels while the authoritative validator requires an exact supplied source path. The existing #48 path safely retained both attempts as `needs-human`; neither is automated-clean evidence. The merged correction places deduplicated supplied paths directly in the graph-review JSON-schema enum and prompt, forbids heading or section suffixes, and retains exact path/quote validation as defense in depth. Its regression captures the production adapter schema and reproduces the heading-qualified `OBJECTIVE — Acceptance` failure without normalizing or overriding it. The full local gate and hosted [Quality run 36103970278](https://github.com/clockgrove/factory-rebuild/actions/runs/36103970278) passed on the reviewed source head. Remaining before use: publish a new immutable artifact containing the merge and produce one fresh read-only plan; this checkpoint does not activate #188 or satisfy #26.

**Field-level graph-review rejection source checkpoint (issue #76):** [PR #79](https://github.com/clockgrove/factory-rebuild/pull/79) merged exact independently reviewed head `48a51661e8acfa9ea2a3165bfb3af27a50296c5b` as main commit `d0b8270f35ab4ea97bb1eb801969ad55dc88b32d`. The graph-review validator independently rejects a non-array response, non-object finding, unknown source, empty or source-absent quote, empty detail, and empty question with the exact failing field path and a fixed reason vocabulary. Private model diagnostics include only that bounded metadata and an optional exact recognized supplied-source label, never provider finding content. Exact quote grounding searches every selected heading packet sharing the exact supplied path, preserving the original validator semantics. The production prompt and schema define `{"findings":[]}` as the unambiguous clean result and forbid advisory or speculative findings merely to avoid an empty array. Existing #48 behavior remains fail-closed: an invalid response becomes one specific `needs-human` planning decision without normalization, retry, Work Item projection, or run state. Focused source tests cover every rejection branch, the clean empty-review path, and duplicate-path heading packets. The full local gate passed build, typecheck, lint, formatting, notices, all 83 tests, package dry-run, and `git diff --check`; hosted [Quality run 36106006519](https://github.com/clockgrove/factory-rebuild/actions/runs/36106006519) and the exact-head GitHub and independent reviews passed without findings. A new immutable public artifact and the same disposable installed-artifact Codex planning smoke remain required before acceptance.

**Public v0.1.11 graph-review acceptance checkpoint:** [PR #80](https://github.com/clockgrove/factory-rebuild/pull/80) released merge `bac3d4303cf4f40be0d4eaa856f43845048af28e` as immutable [v0.1.11](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.11). Hosted Quality passed, and an independent public download verified the 151,296,424-byte tarball at SHA-256 `bfccda87bb4fa1fb7cb65943246d4c95a5c7cc5af27a8f04dc2e61f82244349c`, reproduced it from the exact tag tree, and installed it offline from an empty cache. A distinct public disposable fixture produced an automatically clean read-only plan, completing #76 and #77 without reusing issue #55's target or packets. This artifact does not contain PR #62's provider-neutral harness work and is not issue #55 acceptance.

**GitHub completion checkpoint:** [PR #31](https://github.com/clockgrove/factory-rebuild/pull/31) merged and closed [#22](https://github.com/clockgrove/factory-rebuild/issues/22). Focused interruption tests prove partial issue projection and replayable Work Item/Objective closure after merge and final validation. The gateway verifies issue and PR identity; the atomic snapshot records pending closure without rerunning workers.

**Native delivery checkpoint:** [PR #33](https://github.com/clockgrove/factory-rebuild/pull/33) merged and closed [#21](https://github.com/clockgrove/factory-rebuild/issues/21). Native units prepare conflict-free ready roots after predecessor integration, bounded by operator concurrency and reported driver slots. Publication remains ordered; prepared changes replay onto an advanced integrated head and pass exact validation again. A native execution or validation error records a failed Work Item, and explicit retry rejects a published or ambiguous delivery step. Disposable application tests prove concurrent post-foundation children, a waiting conflict, a later join, terminal native failure, safe retry, and refusal after partial publication. Published-artifact acceptance remains outstanding.

**Changed-file safety checkpoint:** [PR #34](https://github.com/clockgrove/factory-rebuild/pull/34) merged the scoped local prepublication guard for [#24](https://github.com/clockgrove/factory-rebuild/issues/24). It allows unchanged target symlinks/submodules, checks ownership and changed entry modes, blocks new links/special files, and runs packaged Secretlint recommended rules on changed Git and working bytes before branch or LFS publication. Focused real-Git tests cover those paths, a representative LFS candidate, and filtered worker credentials. PR #32 calls the same guard on selected AssetSets after staging and before commit or upload; a selected LFS secret regression test passes.

**Media binding checkpoint (issue #23):** [PR #32](https://github.com/clockgrove/factory-rebuild/pull/32) merged and added structured-only source ingress, durable selection decisions, and explicitly bound downstream member files and descriptors. The merged credential-free gate covers opaque multi-file downstream consumption, selected-media safety scanning, and fresh-clone LFS hydration, plus focused private file and mocked authenticated GitHub attachment ingress. The live GitHub attachment path and exact public artifact still require third-party acceptance.

**Pre-pilot release checkpoint:** [#44](https://github.com/clockgrove/factory-rebuild/issues/44) landed in [PR #45](https://github.com/clockgrove/factory-rebuild/pull/45) and shipped as immutable public `v0.1.1` (source `138d27e8f946caead9cf142080350c20f133ea31`, tarball SHA-256 `38ef800b51513f1e6484b59ee277f3d5742bee8652bd62ad63e36d74309a3da9`). A fresh public download verified and installed offline. Its disposable Objective proved concurrent independent roots and the first two ordinary PR merges, but the combined gate is **not complete**. Result review fell back to human decisions for simple exact-tree facts because its validator accepted quotes only from pinned source text, not from the Git change packet or command-pass evidence, and one malformed finding invalidated the entire batch. [#46](https://github.com/clockgrove/factory-rebuild/issues/46) tracks the narrow correction. This is a real automation gap, not an excuse to count repeated manual approvals as an automatic pass.

**Public v0.1.2 checkpoint:** [PR #47](https://github.com/clockgrove/factory-rebuild/pull/47) merged the #46 result-review correction at `206bf5f06255990e0a155bd60745d29d4ecbdf56`. The [immutable release](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.2) tarball `clockgrove-factory-0.1.2.tgz` has SHA-256 `d5770630221920490370e2d73b8431fa286f569fd136fdd8ac0d9dfb5a285913`. A clean merged-tree build passed all 57 tests, typecheck, lint, formatting, notices, and package checks; a fresh public download matched the built tarball and installed offline from an empty cache. This proves packaging and the narrow code regression, **not** the combined live Objective gate. In the fresh installed disposable gate, three read-only plan previews produced no usable plan: two were stopped after more than ten minutes of silence, and the third returned “Graph review finding lacks a supplied source or question” after about eight minutes. A small structured SDK call completed, so this is not evidence of a blanket SDK outage. No Work Items were projected and no target changes were made. [#48](https://github.com/clockgrove/factory-rebuild/issues/48) tracks the planning-review blocker. The old partially human-approved v0.1.1 run is not substituted for v0.1.2 acceptance.

**Public v0.1.3 checkpoint:** [PR #49](https://github.com/clockgrove/factory-rebuild/pull/49) merged the #48 planning-review correction at `1abe685e4236fe25d694b7ec3601bf5a81c0e900`. The [immutable release](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.3) tarball `clockgrove-factory-0.1.3.tgz` has SHA-256 `eeb83e47f88613f250d7d3e1f66a4c8e5d44c8e6820674df52f638e8371f2cfc`; clean Quality run [35923104643](https://github.com/clockgrove/factory-rebuild/actions/runs/35923104643) passed, and a fresh public download installed offline from an empty cache with the matching `factory@clockgrove` plugin. In fresh public target `clockgrove/factory-v013-gate-20260923-fresh`, Objective #1 compiled graph digest `871652370d966ac6572d7f23f0ce2b2490f9d8528eb8deffad92667e8768a66e`. Capacity prevented a clean graph-review verdict, so the operator accepted that exact inspected graph with the failure retained as `human-accepted`; this is not an automated clean review. Run `946b7bf7-a350-4d68-8d09-f9497d914ed9` projected Work Items #2–#8. Its two dependency-free roots started 17 ms apart from base `ad2274e353e0ae25a2227e170527c242940c3ec0`; `rc-left` produced tree `314dcd849734d8cc1fdf5814bde7622c1f6044c2` and passed its exact validation command. The run then paused before publishing any PR because result review could not verify that the attempt started independently of `rc-right`: the reviewer packet exposed selected-asset observations but omitted the authoritative attempt IDs, start times, dependencies, and execution bases already held in the atomic snapshot. No result was manually accepted, and the target default branch remained at its initial commit. [#51](https://github.com/clockgrove/factory-rebuild/issues/51) tracks the narrow result-review provenance fix. This is real gate evidence, not completion of #44, #46, #48, or #51.

**Public v0.1.4 checkpoint:** [PR #52](https://github.com/clockgrove/factory-rebuild/pull/52) merged the first #51 correction at `12fe7ba3dfc0e60c81f9896e2f0e99a60447e5d5`. The [immutable release](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.4) tarball `clockgrove-factory-0.1.4.tgz` has SHA-256 `49dda22452ed7de569485f76a06672279b69bd56c1ba97fb942146328acd5608`; post-merge Quality run [35942138188](https://github.com/clockgrove/factory-rebuild/actions/runs/35942138188) passed, and a fresh public download installed offline from an empty cache with the matching `factory@clockgrove` plugin. In fresh public target `clockgrove/factory-v014-gate-20260923-fresh`, Objective #1 received a clean automated review for graph digest `344f52d5a135f4be1a9f6265e1c69226c58818cac0fe054ac0a3c4e8ee9ad015`. Run `8259d99e-528d-48a4-a492-7bff43c4e6e9` projected Work Items #2–#8. Its two dependency-free roots started 19 ms apart from original execution base `1ce6fc8296d1563a042153011134621c3458354c`; `rc-left` automatically passed all three criteria using the new delivery observations, and [target PR #9](https://github.com/clockgrove/factory-v014-gate-20260923-fresh/pull/9) merged at `24f37b3594f3f2e56431e5425e5de8302cfd4a98`. Native delivery then replayed the already-prepared `rc-right` change onto that integrated head and correctly advanced its mutable validation and delivery base. The review packet incorrectly labeled that later mutable base as `executionBaseSha`, so the independent reviewer correctly refused to claim that `rc-right` had started from the Objective base. Factory stopped before publishing `rc-right`; no human result override or retry was used. [#51](https://github.com/clockgrove/factory-rebuild/issues/51) was reopened to persist the immutable worker base and integration head observed at attempt start independently of later replay. This is a real test and state-model gap, not completion of #44, #46, #48, or #51.

**Public v0.1.5 checkpoint:** [PR #53](https://github.com/clockgrove/factory-rebuild/pull/53) merged the immutable start-provenance correction at `51c6a5d65a88658c201417bdbb7af23be5099eda`. The [immutable release](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.5) tarball `clockgrove-factory-0.1.5.tgz` has SHA-256 `fd531d7a5da162648d3003dd765fcbbae5d3eb69404727e92a6b18bf0ab765dd`; post-merge Quality run [35946772359](https://github.com/clockgrove/factory-rebuild/actions/runs/35946772359) passed, and a fresh public download installed offline from an empty cache with the matching `factory@clockgrove` plugin. In fresh public target `clockgrove/factory-v015-gate-20260923-fresh`, Objective #1 received a clean automated review for graph digest `54831e376eeff36a3add9f9da414f71b461764511ab9c2e04cd359a656c181ab`. Run `97d08538-ddce-49d7-891d-a44809009639` projected Work Items #2–#8. Its two dependency-free roots started 17 ms apart from original execution base `9d5297780d36440c21040f5db420a03759075df0`, with no integrated head at either start. After `rc-left` merged in [target PR #9](https://github.com/clockgrove/factory-v015-gate-20260923-fresh/pull/9) at `f43c48d1e21439e1674d4900a8bdfc5dc851e27f`, `rc-right` retained the immutable Objective execution base while its mutable delivery base advanced to that merge; its scheduling criterion automatically passed, and [target PR #10](https://github.com/clockgrove/factory-v015-gate-20260923-fresh/pull/10) merged at `e8a1a32d8336aa1eba4e8eeaf378c4a78e858d2b`. This proves the v0.1.5 correction. The run then paused at `rc-stack-middle`: `rc-stack-foundation` published [target PR #11](https://github.com/clockgrove/factory-v015-gate-20260923-fresh/pull/11) with result head `7ca9d592711eed228415c6e460bbc4540a312c80`, and middle started from that exact SHA, but Delivery observations exposed neither the dependency result head nor explicit native unit/layer context. The reviewer correctly requested confirmation rather than inferring the exact-predecessor claim. No result override or retry was used. [#51](https://github.com/clockgrove/factory-rebuild/issues/51) remains open. This is a distinct evidence-contract and regression-coverage gap, not completion of #44, #46, #48, or #51.

**Public v0.1.6 checkpoint:** [PR #54](https://github.com/clockgrove/factory-rebuild/pull/54) merged the predecessor/native-layer observation correction at `4a30935c34a576e910342d96667bb13cc992273c`. The [immutable release](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.6) tarball `clockgrove-factory-0.1.6.tgz` has SHA-256 `8356d2bdb04e1df8795899c9aa779f85d7ff0ddef351176b03380dcd7c1d91f9`; the public download matched and installed offline from an empty cache with the matching plugin. In fresh public target `clockgrove/factory-v016-gate-20260923-fresh`, Objective #1 received a clean zero-revision review for graph digest `58bb9a681789fb8ff46a622d1484fc4dbc5c0a9f9018eb9604e12a7bc1bffbee`. Run `c57938ee-9e98-4b6f-a5ad-86890e9908a4` proved both independent roots, replay, and all three native predecessor/layer criteria automatically; target PRs #9–#12 merged. The run then paused at `rc-lfs-policy` because Delivery observations omitted the accepted graph's owned paths and named resources, so the reviewer could not prove that it was disjoint from acceptance-named peer `rc-stack-foundation`. No result override or retry was used. The investigation also proved that every Codex SDK thread inherited the operator's ambient model and reasoning configuration: the same exact reviewer packet stalled at inherited `xhigh` reasoning but completed in 18 seconds with an explicit `medium` override. The operator's global setting was restored immediately after that bounded diagnostic. This exposes two separate source gaps: missing graph ownership/resource evidence and missing explicit per-role Codex model policy.

**Public v0.1.7 acceptance checkpoint:** [PR #56](https://github.com/clockgrove/factory-rebuild/pull/56) merged the ownership/resource observation correction and explicit per-role Codex model policy at `05efb0752a040d78089abefd0a457d462138e59a`; [Quality run 35955774955](https://github.com/clockgrove/factory-rebuild/actions/runs/35955774955) passed. The [immutable release](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.7) tarball `clockgrove-factory-0.1.7.tgz` is 151,285,898 bytes with SHA-256 `871111f8281399a5facb8b179c9543b26226b721a1543b1ddbab372532f63184`. A fresh public download matched, installed offline from an empty npm cache, exposed the explicit planner/reviewer/worker flags, matched 95 bundled production package versions to the lock, and installed `factory@clockgrove` version `0.1.7` from marketplace ref `v0.1.7`. The isolated configuration persisted `gpt-5.6-sol` with `medium` reasoning separately for all three roles; config digest `cb85cfc8cfe636fa8720fc923aa152d176bf02b70e050503d2b945de74f083f4` bound those choices into the run.

In fresh public target [`clockgrove/factory-v017-gate-20260923-fresh`](https://github.com/clockgrove/factory-v017-gate-20260923-fresh), [Objective #1](https://github.com/clockgrove/factory-v017-gate-20260923-fresh/issues/1) first proved #48's bounded failure path by turning an invalid graph-review finding into a specific decision on graph `da4eaa480269e60a88612f171cbd1fe8e88aa91bf4cfbe43aeab1483233263ec` without projecting issues or creating run state. A separate read-only preview received a clean zero-revision review, while activation's independent recheck raised the out-of-graph package-receipt question before mutation. The operator inspected and accepted the original exact seven-item graph, explicitly grounding package identity in the release tag, source commit, tarball digest, public download, and offline install receipt; this is `human-accepted`, not a claimed automated clean graph review.

Run `ef11c060-5d18-4ee9-bb07-09a17b85d834` projected Work Items #2–#8. `rc-left` and `rc-right` started 20 ms apart at `2026-09-24T04:49:57.696Z` and `2026-09-24T04:49:57.716Z`, both from exact Objective base `acaf56342d82250625bd6ca1b9c292a678a0f678` before integration. Their exact lifecycle criteria passed automatically and target PRs [#9](https://github.com/clockgrove/factory-v017-gate-20260923-fresh/pull/9) and [#10](https://github.com/clockgrove/factory-v017-gate-20260923-fresh/pull/10) merged. Native stack #14 merged exact predecessor layers through PRs [#11](https://github.com/clockgrove/factory-v017-gate-20260923-fresh/pull/11)–[#13](https://github.com/clockgrove/factory-v017-gate-20260923-fresh/pull/13). `rc-lfs-policy` then automatically passed the v0.1.6-blocking disjoint owned-path/resource criterion and merged as [PR #15](https://github.com/clockgrove/factory-v017-gate-20260923-fresh/pull/15), with no result override on any corrected #51 lifecycle/resource criterion.

The media worker produced two complete sets. The operator reviewed both exports, selected whole `candidate-b`, and supplied the intentionally human evidence for unselected-candidate manifests, staging, sidecars, and whole-set selection; selected image SHA-256 is `880c33db690c69b7e14ee9426c4262bb632e112de362843393527a640bc84252`. [PR #16](https://github.com/clockgrove/factory-v017-gate-20260923-fresh/pull/16) merged at final head `020e4eda03574713064a3c744e24efa41c7914f6`. All eight final commands passed; a final exact-tree decision tied the external immutable-package receipt to the complete graph/run record. Factory reports `state=complete`, `objectiveClosure=complete`, and `finalValidation=true`; Objective and Work Item issues are closed. A separate fresh clone at the same head initialized Git LFS locally, hydrated `release/media/selected.png`, reproduced candidate-b's exact digest, and passed all final commands. This completes the exact-public-artifact gate for #46, #48, and #51. It preserves #44's implementation and test evidence, but this Objective did not exercise the required greenfield pnpm-workspace creation path, so #44 remains open.

**Dedicated #44 v0.1.7 gate:** [Objective #17](https://github.com/clockgrove/factory-v017-gate-20260923-fresh/issues/17) started from exact head `020e4eda03574713064a3c744e24efa41c7914f6`, which had no `package.json`, `pnpm-lock.yaml`, or `pnpm-workspace.yaml`. Its bootstrap Work Item produced tree `ed023ac5d41b2db4bd0aadb1a08c4b430b2af138`; the exact source-authorized `pnpm install --frozen-lockfile --ignore-scripts`, `pnpm check`, and `pnpm test` commands all passed. Factory then correctly rejected the result as dirty because pnpm wrote `node_modules/.pnpm-workspace-state-v1.json`; no PR was published. That target-fixture omission was corrected in successor [Objective #20](https://github.com/clockgrove/factory-v017-gate-20260923-fresh/issues/20) by assigning the bootstrap Work Item the exact `node_modules/` ignore rule. A bounded capacity retry produced a clean zero-revision plan with graph digest `aec4c0d4a7946f69d619b2a4deab6d1c2d86678229db18b006e7706ef0404496`, six Work Item commands, and all three final commands. Activation stopped before state or GitHub mutation when a second independent review asked where the final-command authority lived. Investigation proved that v0.1.7 reviewed only the inner graph, omitted command receipts and final commands from that packet, and then made a nondeterministic second model call for an unchanged accepted preview. [#60](https://github.com/clockgrove/factory-rebuild/issues/60) owns the narrow deterministic-activation and complete-review-packet correction. Neither paused Objective is acceptance for #44.

**Public v0.1.8 and #60 checkpoint:** [PR #61](https://github.com/clockgrove/factory-rebuild/pull/61) made the Objective, base, sources, graph, command-authority receipts, and final commands one digest-bound review packet; exact-digest decisions preserve that packet, and activation deterministically verifies it without another planning-model call. [PR #63](https://github.com/clockgrove/factory-rebuild/pull/63) released merged commit `92184854a618bb600e43b03a068cafa0bb13ddfc` as immutable [v0.1.8](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.8). Its 151,286,285-byte tarball has SHA-256 `9f4451fd9b612b2ed937c8d27c3d26f39b001df237d7e1af8d30353830e86a44`; a fresh public download was byte-identical, installed offline from an empty cache, exposed plugin/package version 0.1.8 and marketplace ref v0.1.8, and matched all 95 bundled production package versions to the lock.

Fresh installed [Objective #21](https://github.com/clockgrove/factory-v017-gate-20260923-fresh/issues/21) started from unchanged base `020e4eda03574713064a3c744e24efa41c7914f6`. Its clean zero-revision plan had graph digest `6a094c00696a734aa69395fb3d579257a37db2f343d487c317d4939bb57a084e`; activation verified the accepted packet and projected Work Items #22–#23 in 663 ms with no planning or review model call. The bootstrap worker produced commit `e850e9c6e396fbc445d3aa3cb61d01ed46ade44c`, whose exact tree `098df3992aeb97c0656d2a245ed69ebb4767d68b` contains the intended greenfield workspace and `node_modules/` ignore rule. Factory ran the ordered frozen lifecycle-disabled install, check, and test at that tree; all three exited 0 and the test passed. Result review nevertheless paused because its command evidence carried only command/pass pairs while delivery observations labeled the result commit as `resultHeadSha` and exposed no result tree. The reviewer correctly declined to infer that the successful commands belonged to the separately supplied tree. No result decision, retry, or state modification was used. [#64](https://github.com/clockgrove/factory-rebuild/issues/64) owns the exact-tree receipt and unambiguous commit/tree observation correction. This is not #44 acceptance.

**#64 source-correction checkpoint:** [PR #65](https://github.com/clockgrove/factory-rebuild/pull/65) merged independently audited head `c1d3d4c45cd272a14813dbb335c7ff9c371c9d6c` as main commit `19eedd7b09e3f4011620580e084b664c1be28ef3`. It binds every ordered command receipt to its exact tree, rejects missing, reordered, cross-tree, or command-substituted schemaVersion 2 state, separately names result/integrated commit and tree identities, and gives the reviewer explicit Git object-type guidance. The deterministic integration runs the exact greenfield pnpm command strings at both Work Item and final Objective review and auto-passes both from canonical evidence. All 73 tests and static/package checks passed in hosted Quality run [35969601301](https://github.com/clockgrove/factory-rebuild/actions/runs/35969601301). This is source acceptance, not installed-artifact acceptance; #64 and #44 remain open.

**Public v0.1.9 and greenfield gate checkpoint:** [PR #66](https://github.com/clockgrove/factory-rebuild/pull/66) released merge commit `8ebb44ac96a0b174e6a4169cef80ed1a1a3b0f6b` through annotated tag object `78857dcf4c6dba704bffd43eea2343da2d258bad`, which dereferences to that exact commit. Hosted Quality run [35970327761](https://github.com/clockgrove/factory-rebuild/actions/runs/35970327761) passed. The immutable [v0.1.9 release](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.9) tarball is 151,286,938 bytes with SHA-256 `7cc1ce6aed90cabac6e1fc7d79d5da3f79cfdc5e7ca853ba11481047a6553e62`; it was downloaded publicly, checksum-verified, and installed offline from an empty cache before use. In fresh target [`clockgrove/factory-v019-gate-20260924-fresh`](https://github.com/clockgrove/factory-v019-gate-20260924-fresh), [Objective #1](https://github.com/clockgrove/factory-v019-gate-20260924-fresh/issues/1) run `4c1a0628-fb59-4534-aa9e-6636c82a47a5` automatically accepted both Work Item results, including the exact greenfield pnpm commands. Target PRs [#4](https://github.com/clockgrove/factory-v019-gate-20260924-fresh/pull/4) and [#5](https://github.com/clockgrove/factory-v019-gate-20260924-fresh/pull/5) merged to final commit `c3a795a212d9b9903ca637d8f0605d2e69e2c1f5` and tree `7b082627eb2dbeea81dc9e7c98089e431e6a2ef8`. Final review then waited on the criterion that `proof/follow-up.txt` contain exactly `greenfield pnpm follow-up` without changing a bootstrap-owned path: the final packet exposed aggregate Git evidence and prior reviewer conclusions, but no authoritative per-Work-Item Git delta. No final decision, retry, state edit, or Objective closure was used. [#67](https://github.com/clockgrove/factory-rebuild/issues/67) owns that evidence-contract correction; #44, #64, and #67 remain open.

**#67 source-correction checkpoint:** [PR #68](https://github.com/clockgrove/factory-rebuild/pull/68) merged independently audited head `e1efb36266c5945ff81874fd0195583789c03440` unchanged as main commit `9912188ec444908ca6dd6b61cc6f3ab7e08fc65c`. Final review now receives bounded supervisor-generated sources for each Work Item, tied to accepted ownership, immutable execution and result bases, exact result/integration commits and trees, changed objects, and raw patch excerpts. It excludes nested reviewer prose, rejects truncated or identity-inconsistent evidence, and preserves every rejected candidate with its exact field and reason. The prescribed local gate passed all 75 tests and package checks; hosted Quality run [36035212518](https://github.com/clockgrove/factory-rebuild/actions/runs/36035212518) passed at the exact audited head. Read-only proof construction against the preserved waiting v0.1.9 state produced complete sources for both Work Items without mutating that Objective. This is source acceptance, not released-artifact or live-Objective acceptance.

**Public v0.1.10 acceptance checkpoint:** [PR #69](https://github.com/clockgrove/factory-rebuild/pull/69) released merge `a6f4a2fd79275ac0d60362c5fd36d3fbf97d3d1b` as immutable [v0.1.10](https://github.com/clockgrove/factory-rebuild/releases/tag/v0.1.10). Hosted Quality run [36037001869](https://github.com/clockgrove/factory-rebuild/actions/runs/36037001869) passed. An independent public download verified the 151,290,736-byte tarball at SHA-256 `fe9e24e41f3f56c3ca41df5d69b4b3b0c354ae9613e1e15a00f5f177cf1b72a0`, reproduced it from the exact merge, and installed it offline from an empty cache with package, plugin, dependency-lock, CLI, skill, asset, license, and notice checks passing. Fresh qualifying [Objective #1](https://github.com/clockgrove/factory-v0110-gate-20260924-qualifying/issues/1) activated exact accepted packet `8e9907c241b560fd5807ec5eb3b1a238233844fdddc91f9838ea88aaf30f3c8b` as run `4348dd45-6bfa-44f1-99c5-fe4592cceef1`. Both Work Items and all five final criteria passed automatically from canonical exact-tree receipts and supervisor-generated per-Work-Item deltas, without a human result override or retry. The Objective closed at final commit `069a05279499ecf853cb37f14ca4b26871e89ef0`, tree `9415280b8b349526adbc485f3f1ffacb407e6e40`; issues #44, #64, and #67 are closed.

**Role-default source checkpoint (issue #59):** [PR #71](https://github.com/clockgrove/factory-rebuild/pull/71) resolves installation defaults independently for planner `gpt-5.6-sol`/medium, reviewer `gpt-5.6-sol`/medium, and worker `gpt-5.6-luna`/medium. Explicit per-role flags remain isolated, persisted configurations retain their recorded selections, every Codex SDK adapter receives explicit values, and configuration-digest drift stops activation. The packed-artifact smoke installs the candidate offline and observes those three resolved roles. The complete local and hosted gates passed 76 tests plus package checks, and independent read-only review passed the exact source head. Provider configuration and the installed multi-provider harness seam remain separately owned by #55. This is source acceptance, not a public release qualification.

**Role-default product-boundary correction:** Post-merge audit found that PR #71 also exported a deprecated shared-default compatibility alias even though the clean rebuild provides no backward-compatibility surface. [PR #72](https://github.com/clockgrove/factory-rebuild/pull/72) removes that alias and adds a package-root regression proving that only the three role-specific defaults are exported. The persisted role selections and all #59 runtime acceptance remain unchanged; provider configuration and the installed multi-provider harness seam remain owned by #55.

**Installed harness source-candidate checkpoint (issue #55):** Draft [PR #62](https://github.com/clockgrove/factory-rebuild/pull/62) exposes `composeWithLocalHarness` from the package root and binds a stable adapter identity, exact JSON-safe adapter configuration, declared capability set, and restart-safe durable handle into the existing local execution path and configuration digest. Codex remains the CLI default. The source candidate declares exact optional `@anthropic-ai/claude-agent-sdk@0.3.281` (Anthropic proprietary license) and `@github/copilot-sdk@1.0.13` (MIT) adapters, with provider-specific model/tool/permission/settings/session shapes and developer-local authentication. Missing authentication yields a provider-specific login-and-explicit-retry request; controller GitHub tokens remain excluded. Its credential-free gate includes a fresh tarball installed with optional packages omitted and a non-Codex package-root harness executing one complete production driver/validation/regular-delivery/final-validation path.

An exact-candidate Codex smoke at source `900af484a051a3d5486e27234f48c1c306300195` reused the developer's local login and produced a clean zero-revision one-item plan. The worker changed only its owned file, but inferred a terminal period from prose; Factory's exact controller validation rejected tree `ecc8b603c477247019132f363d81dcfc6decbf30` before delivery. Target main remained unchanged and no PR was created. Independent audit traced the ambiguity to the provider-neutral `workItemPrompt`: the durable Work Item retained exact validation, but the common Codex, Claude, and GitHub Copilot prompt omitted those commands. Corrected exact head `738e4ebecf08e72e0b3a06b53f916287b1b3171c` includes each command with exact provenance/source metadata while leaving execution and authority with Factory, adds shared deterministic coverage, and derives Claude/Copilot telemetry versions from package metadata. Its clean local 95-test/package gate, hosted [Quality run 36110551402](https://github.com/clockgrove/factory-rebuild/actions/runs/36110551402), exact packed artifact, installed surfaces, and independent exact-head review passed. The failed Objective and packet remain immutable and nonqualifying. These changes are unreleased and are not part of immutable public v0.1.11. Issue #55 remains open until a coordinated next release identity and separately approved fresh provider proofs complete.

**Next action:** keep PR #62 draft and coordinate its exact next release identity only after the in-flight trunk work settles; do not rewrite immutable v0.1.11 history or select a conflicting version prematurely. Then rerun the exact package, hosted, and independent source gates for any changed head. Do not retry failed Objective #1. Only after a clean audit and new explicit scope/egress approval, create a distinct fresh Objective in the existing disposable repository for another Codex proof. The real Claude proof remains the required second-provider acceptance for closing [#55](https://github.com/clockgrove/factory-rebuild/issues/55). Because PR #62 additionally ships the GitHub Copilot adapter, prove that adapter live before merging the PR unless the operator explicitly splits or defers it; Copilot does not substitute for Claude's issue gate. The private Clockgrove pilot [#26](https://github.com/clockgrove/factory-rebuild/issues/26) remains gated. Keep provider configuration in #55 rather than #59. Do not start managed, sandbox, Daytona, or other later branches before the trunk and pilot gates. `AGENTS.md` covers building Factory; packaged skills cover using it; the first release uses CLI plus installed skills, not MCP.

**Explicit non-goals now:** managed-agent execution, sandbox providers, Daytona, automatic local/cloud bursting, distributed controllers, media generation providers, and private adopter-specific product work. Factory source must never be its own target. The public fixture files and Objective templates are sufficient to reproduce generic Factory acceptance in a repository the contributor controls.
